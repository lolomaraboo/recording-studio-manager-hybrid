# Phase 3.6 Plan 1: Add Breadcrumb Navigation to 12 Pages

## Objective

Add breadcrumb navigation (back arrow to Dashboard) to 12 pages currently missing it, ensuring consistent UX across all admin pages before marketing launch.

## Execution Context

**Reference implementation:**
`packages/client/src/pages/Clients.tsx:87-91`

```tsx
<Button variant="ghost" size="icon" asChild>
  <Link to="/dashboard">
    <ArrowLeft className="h-5 w-5" />
  </Link>
</Button>
```

**Pattern location:** Inside `<header>` element, before page title (left side of header)

**Required imports:**
- `ArrowLeft` from `lucide-react`
- `Button` from `@/components/ui/button`
- `Link` from `react-router-dom`

## Context

### Pages Missing Breadcrumb (12 total)

**Ressources (4):**
1. `packages/client/src/pages/Talents.tsx`
2. `packages/client/src/pages/Team.tsx`
3. `packages/client/src/pages/Rooms.tsx`
4. `packages/client/src/pages/Equipment.tsx`

**Rapports (3):**
5. `packages/client/src/pages/FinancialReports.tsx`
6. `packages/client/src/pages/Analytics.tsx`
7. `packages/client/src/pages/Reports.tsx`

**Studio (3):**
8. `packages/client/src/pages/Projects.tsx`
9. `packages/client/src/pages/Tracks.tsx`
10. `packages/client/src/pages/Shares.tsx`

**Communication (2):**
11. `packages/client/src/pages/Chat.tsx`
12. `packages/client/src/pages/Notifications.tsx`

### Pages With Breadcrumb (8 total - reference examples)
- Clients, Sessions, Calendar, Invoices, Quotes, Contracts, Expenses, AudioFiles

## Tasks

### Task 1: Add breadcrumb to Talents.tsx
- Read file to locate header structure
- Add ArrowLeft import if missing
- Insert breadcrumb button in header before title
- Verify Button and Link imports exist

### Task 2: Add breadcrumb to Team.tsx
- Read file to locate header structure
- Add ArrowLeft import if missing
- Insert breadcrumb button in header before title
- Verify Button and Link imports exist

### Task 3: Add breadcrumb to Rooms.tsx
- Read file to locate header structure
- Add ArrowLeft import if missing
- Insert breadcrumb button in header before title
- Verify Button and Link imports exist

### Task 4: Add breadcrumb to Equipment.tsx
- Read file to locate header structure
- Add ArrowLeft import if missing
- Insert breadcrumb button in header before title
- Verify Button and Link imports exist

### Task 5: Add breadcrumb to FinancialReports.tsx
- Read file to locate header structure
- Add ArrowLeft import if missing
- Insert breadcrumb button in header before title
- Verify Button and Link imports exist

### Task 6: Add breadcrumb to Analytics.tsx
- Read file to locate header structure
- Add ArrowLeft import if missing
- Insert breadcrumb button in header before title
- Verify Button and Link imports exist

### Task 7: Add breadcrumb to Reports.tsx
- Read file to locate header structure
- Add ArrowLeft import if missing
- Insert breadcrumb button in header before title
- Verify Button and Link imports exist

### Task 8: Add breadcrumb to Projects.tsx
- Read file to locate header structure
- Add ArrowLeft import if missing
- Insert breadcrumb button in header before title
- Verify Button and Link imports exist

### Task 9: Add breadcrumb to Tracks.tsx
- Read file to locate header structure
- Add ArrowLeft import if missing
- Insert breadcrumb button in header before title
- Verify Button and Link imports exist

### Task 10: Add breadcrumb to Shares.tsx
- Read file to locate header structure
- Add ArrowLeft import if missing
- Insert breadcrumb button in header before title
- Verify Button and Link imports exist

### Task 11: Add breadcrumb to Chat.tsx
- Read file to locate header structure
- Add ArrowLeft import if missing
- Insert breadcrumb button in header before title
- Verify Button and Link imports exist

### Task 12: Add breadcrumb to Notifications.tsx
- Read file to locate header structure
- Add ArrowLeft import if missing
- Insert breadcrumb button in header before title
- Verify Button and Link imports exist

### Task 13: Build, deploy and test changes
- Build client Docker image with `--no-cache --pull`
- Deploy to VPS and restart client container
- Use MCP Chrome DevTools to verify breadcrumb appears on all 12 pages
- Test breadcrumb navigation works (returns to /dashboard)

### Task 14: Commit and push changes
- Stage all 12 modified page files
- Commit with message following project conventions
- Push to GitHub main branch

## Verification

**Build verification:**
```bash
docker-compose build --no-cache --pull client
```
- No TypeScript errors
- Build completes successfully

**Deployment verification:**
```bash
rsync -avz packages/client/src/pages/*.tsx vps-n8n:/root/recording-studio-manager-hybrid/packages/client/src/pages/
ssh vps-n8n "cd /root/recording-studio-manager-hybrid && docker-compose restart client"
```
- Client container restarts successfully
- No runtime errors in logs

**Production verification (MCP Chrome DevTools):**
- Navigate to https://recording-studio-manager.com
- Login to test account
- Visit each of the 12 pages:
  1. /talents - breadcrumb present ✓
  2. /team - breadcrumb present ✓
  3. /rooms - breadcrumb present ✓
  4. /equipment - breadcrumb present ✓
  5. /financial-reports - breadcrumb present ✓
  6. /analytics - breadcrumb present ✓
  7. /reports - breadcrumb present ✓
  8. /projects - breadcrumb present ✓
  9. /tracks - breadcrumb present ✓
  10. /shares - breadcrumb present ✓
  11. /chat - breadcrumb present ✓
  12. /notifications - breadcrumb present ✓
- Click breadcrumb on each page → redirects to /dashboard ✓

## Success Criteria

- [ ] All 12 pages have breadcrumb navigation in header
- [ ] Breadcrumb uses exact pattern from Clients.tsx (Button + Link + ArrowLeft)
- [ ] All required imports added to each file
- [ ] Client builds successfully without errors
- [ ] Changes deployed to production VPS
- [ ] All 12 breadcrumbs verified working in production via MCP Chrome DevTools
- [ ] Changes committed and pushed to GitHub
- [ ] Consistent UX across all admin pages before marketing launch (Phase 4)

## Output

12 modified files:
- `packages/client/src/pages/Talents.tsx` - breadcrumb added
- `packages/client/src/pages/Team.tsx` - breadcrumb added
- `packages/client/src/pages/Rooms.tsx` - breadcrumb added
- `packages/client/src/pages/Equipment.tsx` - breadcrumb added
- `packages/client/src/pages/FinancialReports.tsx` - breadcrumb added
- `packages/client/src/pages/Analytics.tsx` - breadcrumb added
- `packages/client/src/pages/Reports.tsx` - breadcrumb added
- `packages/client/src/pages/Projects.tsx` - breadcrumb added
- `packages/client/src/pages/Tracks.tsx` - breadcrumb added
- `packages/client/src/pages/Shares.tsx` - breadcrumb added
- `packages/client/src/pages/Chat.tsx` - breadcrumb added
- `packages/client/src/pages/Notifications.tsx` - breadcrumb added

Deployment artifacts:
- Docker image: rsm-client (latest)
- Production URL: https://recording-studio-manager.com
- Git commit: "Add breadcrumb navigation to 12 pages for consistent UX"

**Estimated duration:** ~20 minutes
- File modifications: 12 min (12 files × 1 min avg)
- Build + deploy: 3 min
- Production verification: 3 min
- Git commit: 2 min
